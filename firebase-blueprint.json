{
  "entities": {
    "QuestionStats": {
      "title": "Question Statistics",
      "description": "Statistics for a specific interview question",
      "type": "object",
      "properties": {
        "viewCount": {
          "type": "integer",
          "description": "Number of times the question was viewed"
        },
        "skipCount": {
          "type": "integer",
          "description": "Number of times the question was skipped"
        },
        "mastery": {
          "type": "number",
          "description": "Mastery score for the question (0-10)"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Last time the stats were updated"
        },
        "userId": {
          "type": "string",
          "description": "The UID of the user who owns these stats"
        }
      },
      "required": ["viewCount", "skipCount", "mastery", "updatedAt", "userId"]
    },
    "QuestionBackground": {
      "title": "Question Background",
      "description": "Deep technical background and architecture overview for a question",
      "type": "object",
      "properties": {
        "content": {
          "type": "string",
          "description": "The generated background content in Markdown"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Last time the background was updated"
        }
      },
      "required": ["content", "updatedAt"]
    }
  },
  "firestore": {
    "/stats/{statsId}": {
      "schema": "QuestionStats",
      "description": "Collection of question statistics indexed by questionId_userId"
    },
    "/backgrounds/{questionId}": {
      "schema": "QuestionBackground",
      "description": "Collection of generated backgrounds for each question"
    }
  }
}
