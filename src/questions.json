[
  {
    "id": "mlp_training",
    "title": "MLP Training Step",
    "description": "Implement the full training step for a Multi-Layer Perceptron (MLP) using PyTorch.",
    "initial_code": "import torch\nimport torch.nn as nn\nimport torch.optim as optim\n\n# --- FIXED STRUCTURE: DO NOT MODIFY ---\nclass MLP(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.layers = nn.Sequential(\n            nn.Linear(10, 32),\n            nn.ReLU(),\n            nn.Linear(32, 1)\n        )\n\n    def forward(self, x):\n        return self.layers(x)\n\ndef train_step(model, optimizer, criterion, x, target):\n    # >>> START YOUR IMPLEMENTATION <<<\n    # 1. Zero the gradients\n    # 2. Forward pass\n    # 3. Compute loss\n    # 4. Backward pass\n    # 5. Optimizer step\n    pass\n    # >>> END YOUR IMPLEMENTATION <<<\n\n# Test setup\nmodel = MLP()\noptimizer = optim.SGD(model.parameters(), lr=0.01)\ncriterion = nn.MSELoss()\nx = torch.randn(1, 10)\ntarget = torch.randn(1, 1)\n# train_step(model, optimizer, criterion, x, target)",
    "initial_message": "I'm Jeff Dean. In deep learning, the training step is the fundamental unit of optimization. We need to ensure we're correctly clearing gradients to avoid accumulation, performing a forward pass to get predictions, computing the loss against targets, and then backpropagating to update our weights. Efficiency and clarity are key here. I've set up a Multi-Layer Perceptron (MLP) training template in the workspace. Please implement the full training step (zero gradients, forward pass, loss computation, backward pass, and optimizer step).",
    "tag": "ml",
    "follow_up": [
      "How would you modify this for a multi-class classification problem?",
      "What changes are needed to implement weight decay (L2 regularization)?",
      "How would you implement gradient clipping to prevent exploding gradients?"
    ]
  },
  {
    "id": "multi_head_attention",
    "title": "Multi-Head Attention",
    "description": "Implement a memory-efficient Multi-Head Attention block from scratch using PyTorch.",
    "initial_code": "import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nimport math\n\nclass MultiHeadAttention(nn.Module):\n    def __init__(self, d_model, num_heads):\n        super().__init__()\n        assert d_model % num_heads == 0\n        \n        self.d_model = d_model\n        self.num_heads = num_heads\n        self.d_k = d_model // num_heads\n        \n        self.w_q = nn.Linear(d_model, d_model)\n        self.w_k = nn.Linear(d_model, d_model)\n        self.w_v = nn.Linear(d_model, d_model)\n        self.w_o = nn.Linear(d_model, d_model)\n\n    def forward(self, q, k, v, mask=None):\n        # q, k, v shape: (batch_size, seq_len, d_model)\n        # >>> START YOUR IMPLEMENTATION <<<\n        # 1. Linear projections\n        # 2. Reshape for multi-head\n        # 3. Scaled dot-product attention\n        # 4. Concatenate heads\n        # 5. Final linear projection\n        pass\n        # >>> END YOUR IMPLEMENTATION <<<\n\n# Test dimensions\nbatch_size, seq_len, d_model, num_heads = 2, 8, 64, 8\nmha = MultiHeadAttention(d_model, num_heads)\nx = torch.randn(batch_size, seq_len, d_model)\n# output = mha(x, x, x)",
    "initial_message": "I'm Jeff Dean. Multi-Head Attention is the backbone of modern Transformer architectures. It allows the model to jointly attend to information from different representation subspaces at different positions. The challenge here is to implement it efficiently, ensuring we handle the projections, head splitting, and the scaled dot-product attention correctly, all while avoiding unnecessary memory allocations or redundant operations. Your task is to implement a memory-efficient Multi-Head Attention block from scratch using PyTorch. Focus on handling dimensions correctly and avoiding unnecessary loops.",
    "tag": "ml",
    "follow_up": [
      "How would you implement causal masking for a decoder-only Transformer?",
      "What is the time and space complexity of this operation in terms of sequence length?",
      "How would you optimize this for very long sequences (e.g., Flash Attention or Linear Attention)?"
    ]
  },
  {
    "id": "batch_norm",
    "title": "Batch Normalization",
    "description": "Implement the Batch Normalization layer for a 2D input (Batch, Features).",
    "initial_code": "import torch\nimport torch.nn as nn\n\nclass CustomBatchNorm1d(nn.Module):\n    def __init__(self, num_features, eps=1e-5, momentum=0.1):\n        super().__init__()\n        self.num_features = num_features\n        self.eps = eps\n        self.momentum = momentum\n        \n        # Learnable parameters\n        self.gamma = nn.Parameter(torch.ones(num_features))\n        self.beta = nn.Parameter(torch.zeros(num_features))\n        \n        # Running statistics\n        self.register_buffer('running_mean', torch.zeros(num_features))\n        self.register_buffer('running_var', torch.ones(num_features))\n\n    def forward(self, x):\n        # x shape: (batch_size, num_features)\n        # >>> START YOUR IMPLEMENTATION <<<\n        # 1. Calculate mean and variance of the batch (if training)\n        # 2. Update running statistics (if training)\n        # 3. Normalize the input\n        # 4. Scale and shift using gamma and beta\n        pass\n        # >>> END YOUR IMPLEMENTATION <<<\n\n# Test\nbn = CustomBatchNorm1d(10)\nx = torch.randn(5, 10)\n# output = bn(x)",
    "initial_message": "I'm Jeff Dean. Batch Normalization is crucial for stabilizing training by reducing internal covariate shift. It normalizes the inputs to each layer so that they have a mean of zero and a variance of one, which helps in faster convergence and acts as a form of regularization. The key is to correctly maintain running statistics during training and use them during inference, while ensuring the normalization itself is numerically stable. Please implement the Batch Normalization layer from scratch for a 2D input. Remember to handle both training and inference modes, and maintain running statistics.",
    "tag": "ml",
    "follow_up": [
      "Why do we use a running mean/variance instead of calculating it during inference?",
      "How does Batch Norm behave with very small batch sizes (e.g., batch size = 1)?",
      "What is Layer Normalization and how does it differ from Batch Normalization in terms of the axes it normalizes over?"
    ]
  },
  {
    "id": "softmax_stability",
    "title": "Numerically Stable Softmax",
    "description": "Implement a numerically stable version of the Softmax function.",
    "initial_code": "import torch\n\ndef stable_softmax(x, dim=-1):\n    \"\"\"\n    Computes softmax in a numerically stable way.\n    x: input tensor\n    dim: dimension along which to compute softmax\n    \"\"\"\n    # >>> START YOUR IMPLEMENTATION <<<\n    # Your code here\n    pass\n    # >>> END YOUR IMPLEMENTATION <<<\n\n# Test\nx = torch.tensor([1000.0, 1001.0, 1002.0])\n# print(stable_softmax(x))",
    "initial_message": "I'm Jeff Dean. Numerical stability is often overlooked, but it's critical in production ML systems. Softmax can easily overflow if the input values are large because of the exponential function. The standard trick is to subtract the maximum value from the input vector before exponentiation, which ensures the largest value becomes zero and prevents overflow. Implement a numerically stable version of the Softmax function. Explain why stability is important and how your implementation addresses it.",
    "tag": "ml",
    "follow_up": [
      "How would you extend this to handle a temperature parameter for controlling the sharpness of the distribution?",
      "What happens if you have extremely negative values in the input? Does that cause underflow issues?",
      "Can you implement the derivative of the softmax function? Why is it useful for backpropagation?"
    ]
  },
  {
    "id": "cross_entropy",
    "title": "Cross Entropy Loss",
    "description": "Implement Cross Entropy Loss from scratch using log-sum-exp for stability.",
    "initial_code": "import torch\nimport torch.nn.functional as F\n\ndef custom_cross_entropy(logits, target):\n    \"\"\"\n    logits: (batch_size, num_classes)\n    target: (batch_size,) containing class indices\n    \"\"\"\n    # >>> START YOUR IMPLEMENTATION <<<\n    # 1. Compute log-sum-exp of logits for normalization\n    # 2. Extract logits of the target classes\n    # 3. Compute the negative log likelihood\n    pass\n    # >>> END YOUR IMPLEMENTATION <<<\n\n# Test\nlogits = torch.randn(3, 5)\ntarget = torch.tensor([1, 0, 4])\n# loss = custom_cross_entropy(logits, target)",
    "initial_message": "I'm Jeff Dean. Cross Entropy Loss is the standard objective function for multi-class classification. It measures the performance of a classification model whose output is a probability value between 0 and 1. When implementing it from scratch, you have to be careful with the log-sum-exp operation to maintain numerical stability and avoid log(0) issues. Implement the Cross Entropy Loss function from scratch. Use the log-sum-exp trick to ensure numerical stability.",
    "tag": "ml",
    "follow_up": [
      "How would you modify this to support label smoothing to improve generalization?",
      "What is the relationship between Cross Entropy and KL Divergence?",
      "How would you handle class imbalance using weighted cross entropy loss?"
    ]
  },
  {
    "id": "knn_scratch",
    "title": "K-Nearest Neighbors",
    "description": "Implement the KNN algorithm from scratch using NumPy, focusing on Euclidean distance vectorization.",
    "initial_code": "import numpy as np\n\ndef knn_predict(X_train, y_train, X_test, k=3):\n    \"\"\"\n    X_train: (N, D) training data\n    y_train: (N,) training labels\n    X_test: (M, D) test data\n    k: number of neighbors\n    Returns: (M,) predictions\n    \"\"\"\n    # >>> START YOUR IMPLEMENTATION <<<\n    # 1. Compute Euclidean distances between each test point and all training points (Vectorized!)\n    # 2. Find the indices of the k nearest neighbors\n    # 3. Get the labels of these k neighbors\n    # 4. Return the most common label (majority vote)\n    pass\n    # >>> END YOUR IMPLEMENTATION <<<\n\n# Test\nX_train = np.random.rand(100, 5)\ny_train = np.random.randint(0, 2, 100)\nX_test = np.random.rand(10, 5)\n# preds = knn_predict(X_train, y_train, X_test, k=3)",
    "initial_message": "I'm Jeff Dean. K-Nearest Neighbors (KNN) is a classic instance-based learning algorithm used for both classification and regression. It makes predictions by finding the $k$ most similar training examples. The core challenge here is computational efficiency: calculating distances between a test point and all training points can be very slow if not vectorized. Your task is to implement the KNN algorithm from scratch using NumPy. You should focus on a vectorized implementation of the Euclidean distance to find the $k$ closest points efficiently.",
    "tag": "ml",
    "follow_up": [
      "How does the choice of $k$ affect the bias-variance tradeoff of the model?",
      "What are the limitations of Euclidean distance in high-dimensional spaces (the 'Curse of Dimensionality')?",
      "How would you implement an approximate nearest neighbor search (e.g., using LSH or KD-trees) for very large datasets?"
    ]
  },
  {
    "id": "logistic_regression_gd",
    "title": "Logistic Regression GD",
    "description": "Implement the gradient descent update rule for Logistic Regression from scratch.",
    "initial_code": "import numpy as np\n\ndef sigmoid(z):\n    return 1 / (1 + np.exp(-z))\n\ndef train_logistic_regression(X, y, lr=0.01, epochs=100):\n    \"\"\"\n    X: (N, D) features\n    y: (N,) targets (0 or 1)\n    \"\"\"\n    N, D = X.shape\n    weights = np.zeros(D)\n    bias = 0\n    \n    def update_step(X_batch, y_batch, w, b):\n        # >>> START YOUR IMPLEMENTATION <<<\n        # 1. Linear combination (logits)\n        # 2. Apply sigmoid\n        # 3. Compute gradients for weights and bias\n        # 4. Return updated weights and bias\n        pass\n        # >>> END YOUR IMPLEMENTATION <<<\n        \n    for _ in range(epochs):\n        weights, bias = update_step(X, y, weights, bias)\n        \n    return weights, bias\n\n# Test\nX = np.random.randn(100, 2)\ny = np.random.randint(0, 2, 100)\n# w, b = train_logistic_regression(X, y)",
    "initial_message": "I'm Jeff Dean. Logistic Regression is the foundation of many classification tasks, modeling the probability of a binary outcome. It uses the sigmoid function to map linear combinations of features to a range between 0 and 1. Gradient descent is the standard optimization approach used to minimize the binary cross-entropy loss. Please implement the gradient descent update rule for Logistic Regression from scratch. You'll need to compute the sigmoid of the logits, calculate the gradient of the binary cross-entropy loss, and update the weights.",
    "tag": "ml",
    "follow_up": [
      "How would you add L1 (Lasso) or L2 (Ridge) regularization to the weight update rule?",
      "What is the difference between Batch, Stochastic, and Mini-batch Gradient Descent in terms of convergence and stability?",
      "How would you handle non-linearly separable data using Logistic Regression without changing the model itself (e.g., feature engineering)?"
    ]
  },
  {
    "id": "kmeans_clustering",
    "title": "K-Means Clustering",
    "description": "Implement the K-Means clustering algorithm, including centroid initialization and iterative updates.",
    "initial_code": "import numpy as np\n\ndef k_means(X, k, max_iters=100):\n    \"\"\"\n    X: (N, D) data points\n    k: number of clusters\n    \"\"\"\n    N, D = X.shape\n    # Randomly initialize centroids\n    centroids = X[np.random.choice(N, k, replace=False)]\n    \n    def assignment_step(X, centroids):\n        # >>> START YOUR IMPLEMENTATION <<<\n        # 1. Assign each point to the nearest centroid\n        pass\n        # >>> END YOUR IMPLEMENTATION <<<\n\n    def update_step(X, assignments, k):\n        # >>> START YOUR IMPLEMENTATION <<<\n        # 2. Recalculate centroids as the mean of assigned points\n        pass\n        # >>> END YOUR IMPLEMENTATION <<<\n    \n    for _ in range(max_iters):\n        assignments = assignment_step(X, centroids)\n        centroids = update_step(X, assignments, k)\n        \n    return centroids, assignments\n\n# Test\nX = np.random.rand(200, 2)\n# centroids, assignments = k_means(X, k=3)",
    "initial_message": "I'm Jeff Dean. K-Means is a fundamental unsupervised learning algorithm for clustering data into $k$ distinct groups. It's an iterative process that alternates between assigning points to the nearest centroid and recalculating the centroids based on the assigned points until convergence. The challenge is to implement these steps efficiently using vectorized operations. Your task is to implement the K-Means clustering algorithm. Focus on the iterative process: assigning points to the nearest centroid and then recalculating the centroids based on the assigned points.",
    "tag": "ml",
    "follow_up": [
      "How do you choose the optimal number of clusters $k$ (e.g., using the Elbow method or Silhouette score)?",
      "What is K-Means++ and why is it preferred over random initialization?",
      "How does K-Means handle outliers and non-spherical cluster shapes? What are some alternatives for those cases?"
    ]
  },
  {
    "id": "decision_tree_gini",
    "title": "Decision Tree Split",
    "description": "Write a function to calculate Gini Impurity and find the best feature split for a decision tree.",
    "initial_code": "import numpy as np\n\ndef calculate_gini(y):\n    \"\"\"Calculate Gini Impurity for a list of labels.\"\"\"\n    # >>> START YOUR IMPLEMENTATION <<<\n    pass\n    # >>> END YOUR IMPLEMENTATION <<<\n\ndef find_best_split(X, y):\n    \"\"\"\n    Find the best split (feature index and threshold) for the data.\n    X: (N, D) features\n    y: (N,) labels\n    \"\"\"\n    best_gini = float('inf')\n    best_split = None\n    \n    def split_data(X, y, feature_idx, threshold):\n        # >>> START YOUR IMPLEMENTATION <<<\n        # Return left and right subsets\n        pass\n        # >>> END YOUR IMPLEMENTATION <<<\n    \n    # Iterate through features and unique values as thresholds\n    # Calculate weighted Gini for each split\n    return best_split\n\n# Test\nX = np.random.rand(50, 2)\ny = np.random.randint(0, 2, 50)\n# split = find_best_split(X, y)",
    "initial_message": "I'm Jeff Dean. Decision Trees are powerful, interpretable models that split data based on feature values to minimize impurity. Gini Impurity is a common metric used to measure the quality of a split by calculating the probability of a random element being incorrectly classified. Finding the optimal split point is the most computationally intensive part of building the tree. Implement a function to calculate Gini Impurity and use it to find the best feature and threshold to split a dataset. This is a core component of building a Decision Tree.",
    "tag": "ml",
    "follow_up": [
      "What is the difference between Gini Impurity and Information Gain (Entropy)? When would you prefer one over the other?",
      "How do you prevent a decision tree from overfitting (e.g., pruning, setting max depth, or min samples per leaf)?",
      "How would you handle categorical features with many levels in a decision tree split?"
    ]
  },
  {
    "id": "feature_scaling_vectorized",
    "title": "Vectorized Feature Scaling",
    "description": "Implement Standardization (Z-score) and Normalization (Min-Max) using NumPy vectorization.",
    "initial_code": "import numpy as np\n\ndef standardize(X):\n    \"\"\"Standardize features to mean=0, std=1.\"\"\"\n    # >>> START YOUR IMPLEMENTATION <<<\n    pass\n    # >>> END YOUR IMPLEMENTATION <<<\n\ndef normalize(X):\n    \"\"\"Normalize features to range [0, 1].\"\"\"\n    # >>> START YOUR IMPLEMENTATION <<<\n    pass\n    # >>> END YOUR IMPLEMENTATION <<<\n\n# Test\nX = np.random.rand(100, 3) * 10 + 5\n# X_std = standardize(X)\n# X_norm = normalize(X)",
    "initial_message": "I'm Jeff Dean. Feature scaling is essential for many algorithms, especially those based on distance metrics or gradient descent, as it ensures that features with larger ranges don't dominate the learning process. Standardization (z-score) and Normalization (Min-Max) are the two most common techniques used to bring features to a comparable scale. Your task is to implement Standardization ($z$-score) and Min-Max Normalization using NumPy. Ensure your implementation is fully vectorized and handles potential division by zero.",
    "tag": "ml",
    "follow_up": [
      "When would you prefer Normalization over Standardization, and vice versa?",
      "How do these scaling methods affect the performance of algorithms like K-Means, SVMs, or Neural Networks?",
      "How would you handle outliers when scaling features (e.g., using RobustScaler with interquartile range)?"
    ]
  },
  {
    "id": "confusion_matrix_metrics",
    "title": "Confusion Matrix & Metrics",
    "description": "Build a confusion matrix from scratch and calculate Precision, Recall, and F1-Score.",
    "initial_code": "import numpy as np\n\ndef compute_metrics(y_true, y_pred):\n    \"\"\"\n    y_true: (N,) ground truth labels (0 or 1)\n    y_pred: (N,) predicted labels (0 or 1)\n    \"\"\"\n    # >>> START YOUR IMPLEMENTATION <<<\n    # 1. Calculate TP, FP, TN, FN\n    # 2. Calculate Precision, Recall, and F1\n    pass\n    # >>> END YOUR IMPLEMENTATION <<<\n    \n    return {\n        \"precision\": precision,\n        \"recall\": recall,\n        \"f1\": f1\n    }\n\n# Test\ny_true = np.array([1, 0, 1, 1, 0, 1])\ny_pred = np.array([1, 0, 0, 1, 0, 1])\n# metrics = compute_metrics(y_true, y_pred)",
    "initial_message": "I'm Jeff Dean. Evaluating classification models requires more than just accuracy, which can be misleading in imbalanced datasets. Confusion matrices provide a detailed view of performance by showing true positives, false positives, true negatives, and false negatives, which are used to derive more informative metrics like Precision, Recall, and F1-Score. Given a list of true labels and predictions, build a confusion matrix and calculate the Precision, Recall, and F1-Score. Do not use scikit-learn.",
    "tag": "ml",
    "follow_up": [
      "When is F1-Score a better metric than Accuracy? Can you give an example of a real-world scenario?",
      "What is the Precision-Recall tradeoff, and how do you visualize it (e.g., PR curve)?",
      "How would you extend these metrics to a multi-class classification problem (e.g., macro vs. micro averaging)?"
    ]
  },
  {
    "id": "iou_calculation",
    "title": "Intersection over Union",
    "description": "Calculate the Intersection over Union (IoU) between two bounding boxes.",
    "initial_code": "def calculate_iou(boxA, boxB):\n    \"\"\"\n    boxA, boxB: list/tuple of [x1, y1, x2, y2]\n    (x1, y1) is top-left, (x2, y2) is bottom-right\n    \"\"\"\n    # >>> START YOUR IMPLEMENTATION <<<\n    # 1. Determine the coordinates of the intersection rectangle\n    # 2. Compute the area of intersection\n    # 3. Compute the area of both boxes\n    # 4. Compute the area of union\n    # 5. Compute IoU\n    pass\n    # >>> END YOUR IMPLEMENTATION <<<\n\n# Test\nbox1 = [0, 0, 2, 2]\nbox2 = [1, 1, 3, 3]\n# print(calculate_iou(box1, box2)) # Expected: 1/7 approx 0.142",
    "initial_message": "I'm Jeff Dean. In Computer Vision, Intersection over Union (IoU) is a critical metric for evaluating object detection models. It measures the overlap between a predicted bounding box and the ground truth box, providing a normalized score between 0 and 1. High IoU indicates that the model has accurately localized the object. Implement a function to calculate the IoU between two bounding boxes defined by their coordinates $(x1, y1, x2, y2)$. ",
    "tag": "ml",
    "follow_up": [
      "How is IoU used in the Non-Maximum Suppression (NMS) algorithm?",
      "What is Generalized IoU (GIoU) and why was it introduced to replace standard IoU in some loss functions?",
      "How would you compute the IoU for a batch of predicted boxes against a batch of ground truth boxes efficiently using vectorization?"
    ]
  },
  {
    "id": "tokenizer_ngrams",
    "title": "N-gram Tokenizer",
    "description": "Write a function to generate n-grams from a text string.",
    "initial_code": "import re\n\ndef generate_ngrams(text, n):\n    \"\"\"\n    text: input string\n    n: size of n-gram\n    \"\"\"\n    # >>> START YOUR IMPLEMENTATION <<<\n    # 1. Clean text (lowercase, remove punctuation)\n    # 2. Split into tokens\n    # 3. Generate n-grams\n    pass\n    # >>> END YOUR IMPLEMENTATION <<<\n\n# Test\ntext = \"The quick brown fox jumps over the lazy dog.\"\n# print(generate_ngrams(text, 2)) # Bigrams",
    "initial_message": "I'm Jeff Dean. Tokenization is the first step in any NLP pipeline, breaking down text into manageable units. N-grams are contiguous sequences of $n$ items from a given sample of text, which are useful for capturing local context and statistical properties of language that simple unigrams might miss. Implement a function that takes a text string and an integer $n$, and returns a list of all $n$-grams. Handle basic punctuation and whitespace.",
    "tag": "ml",
    "follow_up": [
      "How do n-grams help in capturing language structure compared to simple unigrams?",
      "What is the 'sparsity problem' in n-gram models as $n$ increases, and how do we mitigate it?",
      "How would you implement a simple n-gram language model to predict the probability of the next word in a sequence?"
    ]
  },
  {
    "id": "weighted_sampling",
    "title": "Weighted Sampling",
    "description": "Implement a weighted sampling algorithm from scratch.",
    "initial_code": "import numpy as np\n\ndef weighted_sample(items, weights, k=1):\n    \"\"\"\n    items: list of items to sample from\n    weights: list of non-negative weights\n    k: number of samples\n    \"\"\"\n    # >>> START YOUR IMPLEMENTATION <<<\n    # 1. Normalize weights to sum to 1 (probabilities)\n    # 2. Compute cumulative probabilities\n    # 3. Generate random numbers and find corresponding items\n    pass\n    # >>> END YOUR IMPLEMENTATION <<<\n\n# Test\nitems = ['A', 'B', 'C']\nweights = [0.1, 0.6, 0.3]\n# print(weighted_sample(items, weights, k=5))",
    "initial_message": "I'm Jeff Dean. Weighted sampling is a common task in reinforcement learning for experience replay and in data augmentation to handle class imbalance. It involves selecting items from a set where each item has a specific probability of being chosen. Implementing this efficiently from scratch requires understanding cumulative distribution functions. Implement a function that performs weighted sampling. Given a list of items and their corresponding weights, return a sample of size $k$ with replacement.",
    "tag": "ml",
    "follow_up": [
      "How would you implement this efficiently without using `numpy`?",
      "What is the time complexity of your implementation for generating $k$ samples?",
      "How would you handle weighted sampling without replacement? What algorithm would you use (e.g., Efraimidis-Spirakis algorithm)?"
    ]
  },
  {
    "id": "top_k_router",
    "title": "Top-k Router (MoE)",
    "description": "Implement a simple Top-k router for a Mixture-of-Experts (MoE) layer in PyTorch.",
    "initial_code": "import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\nclass TopKRouter(nn.Module):\n    def __init__(self, d_model, num_experts, k=2):\n        super().__init__()\n        self.num_experts = num_experts\n        self.k = k\n        self.gate = nn.Linear(d_model, num_experts)\n\n    def forward(self, x):\n        # x shape: (batch_size, seq_len, d_model)\n        # >>> START YOUR IMPLEMENTATION <<<\n        # 1. Compute logits using the gate linear layer\n        # 2. Find the top-k experts and their scores\n        # 3. Softmax the scores of the selected experts\n        # 4. Return the selected expert indices and their routing weights\n        pass\n        # >>> END YOUR IMPLEMENTATION <<<\n\n# Test\nbatch_size, seq_len, d_model, num_experts, k = 2, 4, 16, 8, 2\nrouter = TopKRouter(d_model, num_experts, k)\nx = torch.randn(batch_size, seq_len, d_model)\n# weights, indices = router(x)",
    "initial_message": "I'm Jeff Dean. Mixture-of-Experts (MoE) is a key architectural pattern for scaling models to trillions of parameters while keeping computational costs manageable. The router is the heart of MoE, deciding which experts should process each token. A simple but effective approach is Top-k routing, where we select the $k$ experts with the highest gating scores. Your task is to implement a Top-k router in PyTorch. You'll need to compute the gating logits, select the top $k$ experts, and normalize their weights using a softmax. Efficiency in these operations is paramount for large-scale training.",
    "tag": "ml",
    "follow_up": [
      "What is the 'expert capacity' problem in MoE, and how do you handle load balancing between experts?",
      "How would you implement a 'noisy' router to improve expert utilization and exploration during training?",
      "What are the communication overheads of MoE in a distributed setting (e.g., All-to-All communication)?"
    ]
  },
  {
    "id": "request_batcher",
    "title": "Efficient Request Batcher",
    "description": "Implement a RequestBatcher class to group incoming string requests into batches based on a specified batch size.",
    "initial_code": "class RequestBatcher:\n    def __init__(self, batch_size: int):\n        self.batch_size = batch_size\n        # >>> START YOUR IMPLEMENTATION <<<\n        # Initialize your internal buffer (bucket) here\n        pass\n        # >>> END YOUR IMPLEMENTATION <<<\n\n    def append(self, request: str):\n        \"\"\"Adds a request to the internal buffer.\"\"\"\n        # >>> START YOUR IMPLEMENTATION <<<\n        pass\n        # >>> END YOUR IMPLEMENTATION <<<\n\n    def get_batches(self):\n        \"\"\"Returns all full batches and clears them from the buffer.\"\"\"\n        # >>> START YOUR IMPLEMENTATION <<<\n        pass\n        # >>> END YOUR IMPLEMENTATION <<<\n\n# --- Test Cases ---\ndef test_batcher():\n    batcher = RequestBatcher(batch_size=2)\n    \n    # Test 1: Partial batch\n    batcher.append(\"req1\")\n    # assert batcher.get_batches() == []\n    \n    # Test 2: Full batch\n    batcher.append(\"req2\")\n    # assert batcher.get_batches() == [[\"req1\", \"req2\"]]\n    \n    # Test 3: Multiple batches\n    batcher.append(\"req3\")\n    batcher.append(\"req4\")\n    batcher.append(\"req5\")\n    # assert batcher.get_batches() == [[\"req3\", \"req4\"]]\n    \n    # Test 4: Remaining partial\n    batcher.append(\"req6\")\n    # assert batcher.get_batches() == [[\"req5\", \"req6\"]]\n    \n    print(\"Tests initialized. Implement the logic to pass them.\")\n\n# test_batcher()",
    "initial_message": "I'm Jeff Dean. In high-performance systems, batching is a critical optimization to reduce per-request overhead and maximize throughput. Whether you're batching database writes or GPU inference requests, the efficiency of your batching logic is key. Your task is to implement a `RequestBatcher` class that groups string requests into batches of size $K$.\n",
    "tag": "algorithm",
    "follow_up": [
      "How would you handle a 'timeout' where partial batches are returned if they've been waiting too long?",
      "How would you make this class thread-safe for concurrent `append` calls using locks or lock-free structures?",
      "If requests had different priorities, how would you adjust your 'bucket' logic to ensure high-priority requests are processed first?"
    ]
  }
]
